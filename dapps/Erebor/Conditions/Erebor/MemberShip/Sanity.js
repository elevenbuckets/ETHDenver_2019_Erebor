module.exports = 
{
        MemberShip_allocateCoreManagers_sanity(addr, jobObj)
        {
                return true;
        },
 	MemberShip_buyToken_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_transferToken_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_bindMembership_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_unbindMembership_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_renewMembership_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addWhitelistApps_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addWhitelistApps_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addPenalty_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_readNotes_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addNotes_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrIsMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrIsActiveMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_tokenIsMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_tokenIsActiveMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrToTokenId_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_pause_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_unpause_sanity(addr, jobObj)
        {
		return true;
        },
 	MemberShip_updateELEMAddr_sanity(addr, jobObj)
        {
		return true;
        },

}
